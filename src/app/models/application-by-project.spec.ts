import { ApplicationByProject } from './application-by-project';

describe('ApplicationByProject', () => {
  it('should create an instance', () => {
    expect(new ApplicationByProject()).toBeTruthy();
  });
});
