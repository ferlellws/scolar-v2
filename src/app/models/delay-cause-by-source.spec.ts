import { DelayCauseBySource } from './delay-cause-by-source';

describe('DelayCauseBySource', () => {
  it('should create an instance', () => {
    expect(new DelayCauseBySource()).toBeTruthy();
  });
});
