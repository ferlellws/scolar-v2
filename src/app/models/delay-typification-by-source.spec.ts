import { DelayTypificationBySource } from './delay-typification-by-source';

describe('DelayTypificationBySource', () => {
  it('should create an instance', () => {
    expect(new DelayTypificationBySource()).toBeTruthy();
  });
});
