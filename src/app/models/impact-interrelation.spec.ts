import { ImpactInterrelation } from './impact-interrelation';

describe('ImpactInterrelation', () => {
  it('should create an instance', () => {
    expect(new ImpactInterrelation()).toBeTruthy();
  });
});
