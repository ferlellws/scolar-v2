import { PmoAssignedByPhase } from './pmo-assigned-by-phase';

describe('PmoAssignedByPhase', () => {
  it('should create an instance', () => {
    expect(new PmoAssignedByPhase()).toBeTruthy();
  });
});
