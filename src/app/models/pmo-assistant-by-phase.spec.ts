import { PmoAssistantByPhase } from './pmo-assistant-by-phase';

describe('PmoAssistantByPhase', () => {
  it('should create an instance', () => {
    expect(new PmoAssistantByPhase()).toBeTruthy();
  });
});
