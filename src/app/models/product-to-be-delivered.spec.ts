import { ProductToBeDelivered } from './product-to-be-delivered';

describe('ProductToBeDelivered', () => {
  it('should create an instance', () => {
    expect(new ProductToBeDelivered()).toBeTruthy();
  });
});
